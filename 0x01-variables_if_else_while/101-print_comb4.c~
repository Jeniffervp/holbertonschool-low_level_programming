#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int fnum, snum, tnum;

	for  (fnum = 48; snum <= 55; tnum++)
	{
		for (snum = fnum + 1; snum <= 56; snum++)
		{
			for (snum = fnum + 1; snum <= 57; snum++)
			{
				putchar(fnum);
				putchar(snum);
				putchar(tnum);
				if (!(fnum == 55 && snum == 56 && tnum == 57))
				{
				putchar(',');
				putchar(' ');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
